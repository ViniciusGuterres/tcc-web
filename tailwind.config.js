/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{js,ts,jsx,tsx}", // Only scan the necessary files
    ],
    theme: { extend: {} },
    plugins: [],
}